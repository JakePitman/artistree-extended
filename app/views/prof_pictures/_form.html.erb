<%= form_with(model: prof_picture, local: true) do |form| %>
  <% if prof_picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prof_picture.errors.count, "error") %> prohibited this prof_picture from being saved:</h2>

      <ul>
      <% prof_picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.hidden_field :image, value: prof_picture.cached_image_data %>
    <%= form.file_field :image %>
  </div>

  <!--<div class="field">
    <%= form.label :profile_id %>
    <%= form.text_field :profile_id %>
  </div>-->

  <div class="field">
    <%= form.label :style_id %>
    <%= form.collection_select :style_id, @styles, :id, :style_name, include_blank: true %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
